<Window x:Class="MM_Localization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MM_Localization"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="350" Width="525">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition Height="20"/>
    </Grid.RowDefinitions>
    <ListView ItemsSource="{Binding dt}" Grid.Row="0">
      <ListView.View>
        <GridView>
          <GridViewColumn DisplayMemberBinding= "{Binding Path=addrOfSeg}" Header="addrOfSeg"/>
          <GridViewColumn DisplayMemberBinding= "{Binding Path=addrInSeg}" Header="addrInSeg" />
          <GridViewColumn DisplayMemberBinding= "{Binding Path=originalText}" Header="originalText" />
          <GridViewColumn DisplayMemberBinding= "{Binding Path=newAddrInSeg}" Header="newAddrInSeg" />
          <GridViewColumn DisplayMemberBinding= "{Binding Path=localizedText}" Header="localizedText"/>
        </GridView>
      </ListView.View>
    </ListView>
    <Button Name="btnSaveFile" Grid.Row="1" Click="btnSaveFile_Click">OK</Button>
  </Grid>
</Window>
